services:
  - type: web
    name: aifusion-campus-hub
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python init_db.py && uvicorn main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/health
